{
  "name": "healthcare-logistics-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Hardhat project for decentralized healthcare logistics contracts",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:polygon-amoy": "hardhat run scripts/deploy.js --network polygonAmoy",
    "deploy:poa": "hardhat run scripts/deploy.js --network poa"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
